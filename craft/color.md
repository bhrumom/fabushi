# Color craft

Use semantic roles rather than raw colors. Accent is scarce and interactive; status colors represent real status. Check foreground/background pairs. Dark mode overrides semantics instead of duplicating component rules.
