# Accessibility baseline

Use semantic HTML/control roles, keyboard reachability, visible `:focus-visible`, descriptive labels, reduced-motion support, and AA contrast for normal text. Generated artifacts must not rely on color alone for state.
