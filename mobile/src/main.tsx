/**
 * RETIRED: the Web/Tauri mobile shell is no longer a build input.
 * Canonical mobile UIs live in mobile/android (Jetpack Compose) and mobile/ios (SwiftUI).
 * Canonical desktop UI lives in desktop/ (Electron).
 */
export {};
