{
  "name": "@fabushi/native-mobile",
  "version": "1.2.36",
  "private": true,
  "description": "Native iOS (SwiftUI) and Android (Jetpack Compose) shells backed by the Mahayana Rust Host.",
  "scripts": {
    "android:assemble": "cd android && ./gradlew assembleDebug",
    "android:test": "cd android && ./gradlew testDebugUnitTest connectedDebugAndroidTest",
    "ios:generate": "cd ios && xcodegen generate",
    "ios:test": "cd ios && xcodebuild test -scheme Fabushi -destination 'platform=iOS Simulator,name=iPhone 16'"
  }
}
