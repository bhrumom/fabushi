{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the trusted Fabushi application shell. Marketplace plugins never receive direct Tauri access; they must use the Mahayana Rust Host bridge.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
