# 验收追踪矩阵

| Requirement | Evidence target | State |
|---|---|---|
| Chrome is an independent Fabushi platform, not the legacy extension | `chatgpt-vps-control/chrome-platform/extension/**`; new production packager source | verified on branch |
| Desktop-aligned Fabushi surface | `app.html`, `app.css`, `app.js`: Chats/小程序/Marketplace/Browser/Settings/account/search | verified by validator/tests |
| Correct MV3 wiring | `chrome-platform/extension/manifest.json` + validator | verified |
| No userscript production coupling | no `userScripts`, no `.user.js`, no legacy `userscripts.js` in Web Store ZIP | verified |
| Reuse desktop signed-in account without copying long-lived credentials | `platform-bridge.js` + `chrome-platform-server.cjs`; desktop Host owns auth/session | verified by contract tests |
| Desktop Settings handoff without Chrome custom-protocol navigation | `desktop.settings.open` Native Messaging request; OS opens `fabushi://settings/...` | verified by implementation/contract; packaged GUI smoke pending |
| Control current user Chrome without a second CDP browser | existing browser native bridge + `browser-control.js` + extension browser sessions | verified by bridge/CDP tests; packaged GUI smoke pending |
| Marketplace platform isolation | desktop server forces `platform=chrome-extension` | verified by tests |
| Store prep | first-class package script + privacy/assets/release docs | verified |
| Production ZIP | exact seven-file allowlist, SHA-256, Actions artifact | verified by GitHub Actions |
| Store publication | developer dashboard upload/review/public smoke | publisher-controlled, pending |
