# 质量与测试策略

Required branch checks: JSON parse manifest, Node syntax-check extension JS and packaging script, validate referenced local resources, assert action popup/service worker exist, package production ZIP, inspect archive contents, and run any existing extension tests discovered in repository.
