# 安全隐私与合规

No remote executable code. Host access remains limited to ChatGPT origins already required by the bridge/userscript runtime. Broad Chrome permissions must be justified individually in store review docs and reduced later if runtime evidence proves they are unnecessary. Credentials/tokens are not embedded in the package.
