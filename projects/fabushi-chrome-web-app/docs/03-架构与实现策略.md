# 架构与实现策略

Use the existing `chatgpt-vps-control/extension` MV3 service worker and `app.html` action popup as the product entry. Keep UI self-contained with no remote code. Existing userscript runtime remains untouched. Store packaging copies only explicitly approved runtime files into a clean staging directory and zips that directory.
