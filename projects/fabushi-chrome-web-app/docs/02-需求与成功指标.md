# 需求与成功指标

| ID | Requirement | Success measure |
|---|---|---|
| CWA-R001 | 一个 Fabushi MV3 扩展承载产品 UI、浏览器桥和自动化脚本 | 当前单调版本、白名单 ZIP、UI smoke |
| CWA-R002 | 兼容旧 Bridge 九命令、八动作及标签/CDP 事件 | contract test 每项命令/动作/事件存在且语义一致 |
| CWA-R003 | 普通 HTTP(S) 标签基于标题、URL、generation 精确认领 | stale title/url/generation 均 fail closed |
| CWA-R004 | 保留 CDP 串行化、OOPIF、下载和 tab ownership 生命周期 | extension journey 全部通过 |
| CWA-R005 | Chats/Mini Apps/Marketplace/设置和脚本启停不回归 | Electron/Chrome UI journey 通过 |
| CWA-R006 | 账号凭据留在桌面 Host；origin/secret/输入受限 | security contract 无泄漏/伪造连接被拒 |
| CWA-R007 | 可复现打包、来源 SHA、权限说明和 Web Store 单调版本 | exact-SHA package/release evidence |
| CWA-R008 | 迁移后旧 Bridge/官方 ChatGPT 从 Default/Profile 2 删除 | Chrome UI inventory 只剩 Fabushi 相关能力 |
| CWA-R009 | Chrome 插件独立登录现有 Fabushi 账号 | 登录/重启失效/重新登录旅程通过；不存 refresh token |
| CWA-R010 | 官方 MCP 只发现并控制同账号 Chrome | A 账号可控，B 账号不可见，退出后立即离线 |
| CWA-R011 | 远程直连复用完整 Bridge 命令和事件 | 九命令、八动作、CDP/OOPIF/下载/事件契约通过 |
| CWA-R012 | exact-SHA 交付并部署官方网关 | CI 证据、Release/Web Store、生产同账号旅程通过 |
| CWA-R024 | 插件主产品壳实现 Grok Bot 0.18 的完整 Agent UI/信息架构 | full-page extension app 的 roster/conversation/transcript/composer/tools/MCP/settings/context UI 与批准参考一致 |
| CWA-R025 | 对 pinned Grok `source/**` + `frontend/**` 做逐源文件审计与归宿 | ledger 100% 覆盖；不要求目标文件一对一；无静默遗漏 |
| CWA-R026 | 每个 Grok 产品责任都有 Chrome 真实实现/现有等价实现/经审查平台替代 | production wiring + contract/E2E 证据；禁止 placeholder/no-op |
| CWA-R027 | Renderer -> Platform Runtime -> Coordinator -> Host -> Runner 边界真实存在 | 架构 gate、独立合同测试、renderer 不直连 Host/Runner |
| CWA-R028 | MV3 Service Worker 只做 transport/capability broker | worker restart 后重新连线并 resync；durable run truth 不在 worker 内存 |
| CWA-R029 | 桌面 Native Messaging 不在线时仍可完整使用核心 Agent | remote Coordinator/Host 路径通过普通 chat、streaming、tool/MCP E2E |
| CWA-R030 | 保留现有 Browser Control、CDP/OOPIF/下载/tab lifecycle | 九命令、八动作和现有安全合同在最终架构零回归 |
| CWA-R031 | 保留 Mini Apps、Marketplace、userscript 导入/更新/启停/恢复 | 最终 Grok UI 内的原功能 packaged journey 全通过 |
| CWA-R032 | 保留 Fabushi 浏览器登录、同账号 MCP、跨账号拒绝和退出撤销 | account/MCP packaged security journey 全通过 |
| CWA-R033 | Grok Agent turn 支持 accepted/thinking/tool-running/streaming/terminal 和 cancel/retry/recovery | exact-HEAD packaged trace + UI evidence |
| CWA-R034 | app/Service Worker 重载时恢复同一个 durable Run | 同 runId/generation resync，无任务丢失、无重复 tool/send |
| CWA-R035 | toolbar action 打开完整 Grok-shaped extension app；popup 只可为辅助 | packaged Chrome UI 证明完整主界面不受 popup 尺寸限制 |
| CWA-R036 | Grok parity 不降低现有扩展安全边界 | CSP/origin/session/generation/native-host/userscript 安全 gate 全通过 |
| CWA-R037 | Grok Bot Chrome effect 达成 | accepted -> thinking -> real tool/MCP/browser action -> live result -> continued inference -> streaming -> terminal，并通过 same-run recovery |

成功定义现由 `docs/08-grok-bot-0.18-extension-architecture-product-parity.md` 与本表共同约束。
现有 CWA 功能全部保留只是基础 gate；只有 CWA-R024..R037 及原有必需 release/security/
migration gates同时满足，插件版才可宣称完成 Grok Bot 0.18 等价实现。
