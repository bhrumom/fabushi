# 完成定义与验收

Branch implementation is accepted when code/docs/package script are committed and executable checks pass. Repository task is not canonically complete until required PR/main/post-main gates in root AGENTS.md are satisfied; Web Store publication additionally requires publisher account and manual listing/review actions.
