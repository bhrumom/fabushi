# Source research

Startup research reviewed GoogleChrome `chrome-extensions-samples` Manifest V3 patterns and current Chrome extension publishing guidance. Decision: keep MV3 service worker + action popup, avoid remote code, package only repository-local runtime assets, and document every broad permission for store review. Apache-2.0 samples were used as architectural reference only; no source code copied.
