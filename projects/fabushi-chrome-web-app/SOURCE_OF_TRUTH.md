# Source of truth

User requirement received 2026-09-07: stop extending userscript functionality; only complete the Chrome extension Fabushi APP and Chrome Web Store preparation on `codex/fabushi-chrome-userscripts`, using GitHub App writes and commits, no clone. Preserve existing userscript code without expanding it.

Clarification received 2026-09-07 19:21 +08:00 supersedes the earlier implementation shape: Chrome must be a **new first-class Fabushi platform**, not a continuation of the previous ChatGPT/Computer Control extension UI. The Chrome platform must live independently from the legacy extension implementation, present the same Fabushi product shell as desktop (Chats, Mini Apps, Marketplace, account/settings surfaces), reuse the signed-in desktop account through a local native bridge without copying long-lived credentials into extension storage, let the extension open/focus the desktop Settings surface directly, and let desktop/browser-control code operate the user's existing Chrome tabs through the extension so a separate CDP-launched browser is not required. Existing userscript code is out of scope and must not be extended or loaded by the new platform.
