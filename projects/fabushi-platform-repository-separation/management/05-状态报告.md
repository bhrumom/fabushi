# 状态报告

## 2026-09-18 — PRS-001 启动

- 已从 GitHub canonical `main` 读回 `cbe65975f3c4c077fa64af4171ebe3d2900185ad`。
- 已从实时 `PORTFOLIO.json` 读回 `next_sequence: 13`；本任务分配 `FAB-P0013` / `PRS`。
- 已核对当前公开仓库和已存在的 Userscript 仓库；新平台仓库默认保持 public。
- 已完成开源优先调研：GitHub `git-filter-repo` split 文档、Git `git-subtree`、Josh repo
  filtering；决策记录于 `decisions/ADR-0001-repository-split-toolchain.md`。
- 已建立目标平台矩阵，加入独立 `fabushi-cli`，并标出 shared Core、平台源码、legacy/未分配
  路径和依赖耦合。
- 当前阶段：M0，registration/bootstrap PR #2705 已创建；尚未创建目标 repo、导出代码或修改生产入口。
- 本机未执行构建、打包、原生测试、E2E 或历史重写；重型操作计划放 GitHub-hosted CI。
- 下一步：完成 PR #2705 的 portfolio validator、审阅和合入；随后创建目标 repos 并执行边界化设计。

## 2026-09-18 — PRS-003 bootstrap repositories

- PR #2705 已在 canonical `main` 合入，merge SHA 为 `010841fe9a3a6d7c2491fb3c6e641b0977cba912`；`FAB-P0013` registry readback 通过。
- 已创建 13 个 public GitHub repositories，并为每个仓库建立独立 `main` 与迁移说明：Core、CLI、Web、Desktop、Android、iOS、WeChat、Chrome Extension、Backend、Forum、Commerce、Marketplace、Governance。
- `bhrumom/fabushi-cli` 已作为独立目标仓库建立；CLI 源码和 release 仍待后续边界化/导出，不与 Core 混放。
- 已有 `bhrumom/fabushi-chatgpt-auto-confirm-userscript` 继续复用，不复制成第二个仓库。
- 当前阶段：M1；仓库 bootstrap 完成，branch protection、源码导出、独立 CI、打包/E2E 和 Release 尚未完成。
- 下一步：提交 PRS-003 记录 PR，配置保护规则，然后在 GitHub-hosted fresh mirror 执行 Core/CLI/平台导出。

## 2026-09-18 — PRS-005 export workflow

- 新增手动触发的 `Platform repository export` workflow，按目标矩阵过滤 canonical source SHA，并输出 source/target refs、路径清单、fsck 和 summary artifact。
- workflow 默认 dry-run；只有显式关闭 dry-run 才会使用现有 `OFFICIAL_SITE_RELEASE_PAT` 对目标仓库 `main` 执行受控 force-push。
- CLI 与 Core 的 CLI-specific crate 排除规则已显式写入 workflow，避免 shared Core 与独立 CLI 再次混放。
- 当前状态：workflow PR 待提交；尚未执行源码导出或目标仓库 force-push。

## 2026-09-18 — PRS-005 formal platform exports

- workflow 已通过 PR [#2709](https://github.com/bhrumom/fabushi/pull/2709) 合入；README 保留修复通过
  PR #2710，shell 模板元数据修复通过 PR #2711；当前 canonical source SHA 为
  `7851b689d2fe3fc3893cd9f4363899cc4a03e83b`。
- CLI、Core 以及其余 11 个目标均已由 GitHub-hosted workflow 以 `dry_run=false` 正式写入各自仓库的
  `main`。共 13 个 runs 成功，目标提交、路径数量、artifact 和回滚引用已记录在
  `evidence/PRS-005-platform-export-20260918.md`。
- 已回读每个目标仓库的 `MIGRATION_SOURCE.md`：源仓库、源 SHA、目标边界、source roots 和项目编号均正确；
  CLI 独立于 Core，现有 Userscript 仓库继续复用而未重复复制。
- 每个目标保留 `refs/backup/prs-bootstrap-20260918`，可回滚到导出前的 bootstrap 提交；源仓库未删除任何
  产品目录或历史。
- 当前阶段：M3 `in-progress`。本阶段的源码物化/路径证据已完成；独立仓库的 branch protection、CODEOWNERS、
  CI、依赖版本化、构建/E2E/Release 和生产入口切换仍未完成。
- 本机未执行应用构建、测试、历史重写或打包；重型验证继续由各目标仓库的 GitHub Actions 承担。
- 下一步：逐仓配置治理和 CI，先固化 Core/CLI 版本化依赖边界，再推进平台独立构建与发布验收。


## 2026-09-21 — PRS-009 cross-repository routing + Spec-first guardrail

- Initial Desktop-only routing requirement was expanded by the user to all split projects, explicitly including Android, iOS/mobile and CLI.
- Legacy root `AGENTS.md` now uses the full PRS inventory routing table instead of a Desktop-only redirect.
- Scope includes all 13 formal target repositories plus the pre-existing standalone ChatGPT auto-confirm userscript repository.
- Every target repository must carry its own root `AGENTS.md` and durable Spec-first policy/template. Desktop already satisfies this from PR #15 in `bhrumom/fabushi-desktop`; the remaining repositories are being aligned.
- The rule is fail-closed: migrated product code still present in `bhrumom/fabushi` is reference/migration material and must not receive current product fixes.
- Status: cross-repo implementation in progress; legacy-source PR #2723 will be updated and target-repo changes merged/read back before PRS-009 is marked passed.
