# 变更日志

## 2026-09-18

- 新建 `FAB-P0013` / `PRS`，将“每个平台独立 GitHub 仓库”登记为独立迁移项目。
- 固定源 canonical SHA `cbe65975f3c4c077fa64af4171ebe3d2900185ad` 和 public 仓库默认假设。
- 建立 Core、Web、Desktop、Android、iOS、WeChat、Browser、Backend、CLI、Forum、Commerce、
  Marketplace、Governance 的目标矩阵。
- 记录先 Core/CLI/边界化、再导出、再独立 CI/Release、最后源仓库治理-only 收敛的迁移顺序。
- 已创建治理登记 PR #2705；目标仓库创建和源码导出继续保持在合入后的后续阶段。
- 已创建 13 个 public bootstrap repositories，包含独立的 `fabushi-cli`；每个仓库的 URL、main bootstrap SHA 和迁移状态写入平台矩阵。
- 新增 GitHub-hosted `Platform repository export` workflow；默认 dry-run，显式 push 才会更新目标仓库，证据保留 90 天。
- workflow 修复并合入后，以源提交 `7851b689d2fe3fc3893cd9f4363899cc4a03e83b` 完成 13 个目标仓库的正式导出；CLI 与 Core 保持独立，所有目标 `main`、路径清单、artifact 和 bootstrap 回滚引用已写入 `evidence/PRS-005-platform-export-20260918.md`。
- M3 进入 `in-progress`：源码物化已完成，但各独立仓库的 branch protection、CODEOWNERS、CI、依赖解耦、构建/E2E/Release 和生产切换尚未完成。


## 2026-09-21 — PRS-009 Desktop repository routing guardrail

- Added a repository-level routing guardrail to prevent AI agents from treating legacy Desktop paths in `bhrumom/fabushi` as current product source.
- Canonical Desktop development repository is now explicitly `bhrumom/fabushi-desktop`.
- Desktop implementation tasks must fail closed in this source repository and switch to the Desktop repository before editing code; only migration/governance/history work explicitly targeting `bhrumom/fabushi` may edit Desktop-related records here.
