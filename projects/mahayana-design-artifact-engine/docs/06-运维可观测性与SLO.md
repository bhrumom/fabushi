# 运维可观测性与SLO

Track design package resolution failures, skill staging failures, artifact preview denial, exporter unavailable/error, and runtime-profile detection latency. No new remote service is required. SLO ownership follows Mahayana/Desktop existing telemetry contracts; define concrete thresholds when production counters are wired.
