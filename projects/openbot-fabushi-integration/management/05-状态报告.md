# 状态报告（append-only）

## 2026-09-13 — OBF-001 round start

Locked source baseline: Fabushi `main@25aaf4fef98fb33ca0100c8cdbd6c7971390ec94`; OpenBot `19fdcbb7fd5c072d5c2aa95800fcbbbf72aa202c`. Existing PR #2580 is treated as reusable baseline, not as proof of complete parity. Initial matrix identifies a real ordinary-message structured-result/source-chip rendering gap and several evidence/UX parity items that still require packaged proof. Status remains `IN_PROGRESS`.

## 2026-09-13 — verified gap implementation prepared

Repository inspection found two additional concrete parity regressions: roster shape was mutated by `nth-child` CSS instead of remaining identity-derived, and reply-work CSS targeted stale aliases rather than live `agentThinkingRow` / `agentActionRow`. OBF-001 removes those overrides, adds only generic runtime-result/source-chip/hover presentation, and introduces a production-only Playwright acceptance. The acceptance is deliberately gated by `OBF_REAL_ACCEPTANCE=1` and a supplied reference screenshot; a skipped or test-host run is explicitly non-acceptance. Packaged proof, measured screenshot diff, full video, trace/log bundle and exact post-main Release are still pending. Status remains `IN_PROGRESS`.

## 2026-09-13 — rebased onto moving canonical main

Canonical main advanced during this round to `9365b5e13f1fb5ac7b31f00049e487ab2174edbc` via PR #2583, which also advanced the release train to 1.2.61. OBF was rebuilt on that exact main rather than carrying forward stale release workflow content; the OBF candidate is therefore 1.2.62 with Android/iOS build number 31. The first OBF CI attempt also exposed an existing App Surface generation flake plus an OBF login-helper race; only the OBF login helper was repaired here. Status remains `IN_PROGRESS`.
