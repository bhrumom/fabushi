# OBF-001 Visual regression specification

Reference input: user-supplied OpenBot screenshot `2048x1280`, SHA-256 `555f8ae43517b2f6b2a1e545a16c0eb95e596585a1c9adfd211cad6a217c475b`. The browser/account chrome is not an app-controlled surface and is excluded. The current manually bounded OpenBot app-controlled crop is `(x=176,y=196,w=1671,h=937)`, crop SHA-256 `0a94bcf48630f4d872bab2e765df8df396026ac08d2b6c3b5fe64b112c1d268d`. The crop itself is the comparison surface; it is **not** stretched or normalized. The exact packaged Fabushi BrowserWindow content area is therefore fixed to `1671x937`, dark color scheme, reduced motion. The acceptance test receives that exact app-controlled crop through `OBF_REFERENCE_SCREENSHOT`; it refuses to manufacture a substitute and runs only when `OBF_REAL_ACCEPTANCE=1` plus a real `FABUSHI_ELECTRON_EXECUTABLE` are provided.

Visual gate:

- Playwright screenshot comparator threshold `0.15` for per-pixel color tolerance;
- maximum differing pixel ratio `0.08` (8%) across the app-controlled workspace;
- critical geometry assertions are independent of pixel tolerance: roster/sidebar width, avatar box `34px +/-2px`, final three-column table, source chips, hover controls and composer must all exist in the same runtime scene;
- identity is exact rather than approximate: the same canonical Bot ID must report the same `data-shape` in roster, header, transcript/action surface and after restart;
- any non-zero accepted pixel difference is recorded as a measured residual and must be listed by region before any wording such as “1:1” is allowed. Zero-diff is the only basis for claiming literal 1:1.

Evidence emitted by the real acceptance spec: Fabushi comparison screenshot, Playwright trace, Mahayana workbench snapshot, lifecycle status journal and screenshot-diff artifacts. The surrounding packaged workflow must also retain the reference screenshot, full-session video and application/runtime logs. PR test-mode runs and skipped production acceptance tests are never substitutes for this post-main evidence.
