# 完成定义与验收

Status may become `complete` only when all are true: (1) matrix has no unclassified fusible feature; (2) implementation reuses Fabushi equivalents and only verified gaps were changed; (3) PR merged through repository governance; (4) exact-main fresh packaged Desktop run at fixed dark theme/reference viewport executes Chief/Research/Builder/Launch with real Mahayana events; (5) final brief has the requested table/source chips/hover actions/composer; (6) stable avatar shape is proven across roster/header/transcript/restart; (7) visual diff is measured against the reference and residual differences are enumerated if non-zero; (8) same-run screenshot/video/trace/log bundle exists.
