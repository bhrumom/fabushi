# Source of truth

Identity is `FAB-P0009` / `DBD`. Canonical engineering state is GitHub `bhrumom/fabushi` `main`, `projects/PORTFOLIO.json`, and this folder. Persisted user requirements in `source/` precede normalized specs; accepted ADRs precede management plans. Live GitHub PR/CI/Release/deployment facts decide implementation state. The authenticated Douyin session is an intake source only and must not expose cookies in repository records. Conflicts are recorded and corrected explicitly; chat memory never silently overrides canonical files.
