# 需求与成功指标

- `DBD-REQ-001`: accept newline/JSON batches of Douyin short links and canonical video links; deduplicate by aweme ID or canonical URL.
- `DBD-REQ-002`: resolve and download the highest-quality non-watermarked MP4 exposed to the authorized session, with bounded concurrency, delay, retries, safe filenames, and no overwrite unless requested.
- `DBD-REQ-003`: produce a machine-readable manifest containing source URL, aweme ID, title, output path, bytes, digest, status, and failure reason without credentials.
- `DBD-REQ-004`: appear in Fabushi marketplace search for “抖音”, “无水印”, and “下载”, and install/open through the canonical package flow.
- `DBD-REQ-005`: collect every video share visible in the “小李子” conversation, download it locally, and reconcile requested/succeeded/failed counts.

Success metrics: 100% deterministic contract fixtures; no secret/cookie leakage; all collected items accounted for; exact-main packaged E2E and visual evidence retained; marketplace browse/install verified on the released build.
