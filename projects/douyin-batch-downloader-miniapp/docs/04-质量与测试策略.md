# 质量与测试策略

CI-only heavy validation: Rust unit tests for URL normalization, JSON extraction, candidate ranking, filename safety, dedupe, and partial failure; Node marketplace search/release/install contracts; plugin manifest validation; a mocked HTTP download integration; packaged Electron simulated-user marketplace search/install/open journey with step screenshots, full video, trace, HTML results, and logs on `always()` retention. A gated live Douyin dispatch may use repository secrets/session artifacts but must redact credentials. No local build, Rust test, application test, E2E, or package run is permitted.
