# Seventh round — fail closed on the final review P1

Project `FAB-P0003 / FCM`; task `FCM-FAST-20260911`; PR #2503.

## Why the queue was stopped

After current-main synchronization and before accepting the PR, the independent review history was re-read. The latest exact candidate review on `498f5d67176f390d628b43e623db37344553c002` contained a P1: `Messaging Product Gate` only triggered on Feature Host/social/protocol package paths, while `mahayana-feature-host` compiles a wider Mahayana workspace dependency graph. A dependency change such as `mahayana-core`, `mahayana-computer`, `mahayana-harness`, `mahayana-host`, the workspace manifest or lock could therefore bypass the product gate entirely.

The PR had already entered the protected merge queue after the new `automerge` authorization. GitHub correctly rejected a branch update while queued. The automation label was removed and PR #2503 was temporarily closed to leave the queue before making the safety repair. No protected-main change was made from the faulty candidate.

## Repair

Commit `1eefc51617dec604b9a94f545f89e8a79e74f67c` changes `.github/workflows/messaging-product-gate.yml` so both pull-request and canonical-main path triggers cover the complete `third_party/mahayana/mahayana-rs/**` workspace. Impact selection keeps `mahayana-social`'s stronger messaging+host+desktop dependency behavior and conservatively selects Host + desktop contracts for every other Mahayana workspace source/manifest change. This removes dependency-trigger false negatives; it intentionally accepts some extra testing until a metadata-derived dependency closure can prove an equally safe narrower selection.

Commit `fd05a981089c7d2881b900a962818e33cfa23d6d` updates the protected automerge controller with the same broad Mahayana workspace dependency boundary. Therefore a Mahayana dependency PR cannot be enqueued based on `CI` while its newly triggered `Messaging Product Gate` is still incomplete.

## Acceptance state

This P1 is source-repaired but not yet accepted. The PR must be reopened on the later exact head, receive a fresh Actions wave and independent exact-head review, then re-enter the protected merge queue only if the required CI, Messaging Product Gate and Electron product gates are green. Earlier successful runs and the aborted queue entry are retained as history but do not prove the repaired head.
