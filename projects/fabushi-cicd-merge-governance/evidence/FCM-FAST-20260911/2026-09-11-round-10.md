# FCM-FAST-20260911 — Round 10 evidence

Date: 2026-09-11
Scope: exact-head evidence hardening, workflow-trigger hardening, Shopify-style affected-test follow-up staging, and canonical-main synchronization.

## Independent review findings closed in code

The workstream was kept fail-closed after earlier green runs. Review threads showed that UI evidence could still accept syntactically plausible but unusable artifacts and that an automerge-only policy change could bypass the new fast-feedback workflow.

Repairs on PR #2503:

- indexed PNG pixels are unpacked after unfiltering and every index must resolve to a declared PLTE entry;
- checkpoint JSON must validate as the actual `AppSnapshot`/ElementState schema rather than merely parse as JSON;
- Playwright trace archives must pass ZIP CRC checks, contain bounded readable `.trace` JSONL, and include reconstructable action history;
- WebM evidence must be fully decoded, contain frames/non-zero duration, and cover the recorded journey duration within a bounded tolerance;
- `.github/workflows/automerge.yml` is included in PR/main triggers for `Fabushi CLI-first fast feedback` so its own regression tests cannot be skipped by an automerge-only change;
- earlier review findings for Mahayana/messaging dependency triggers, media enqueue gating, and untracked exact-source provenance were read back in current code and replied to with current evidence.

## Objective failure retained instead of hidden

At exact head `fdffe1f43e892cfe6202124f0d16b05dd5f85791`, CLI-first run `34607039129` produced:

- contract lane: success;
- renderer semantic Playwright journeys: success;
- UI evidence verifier: failure;
- retained UI artifact: `10266402428`, digest `sha256:5b0073836bc5f3008d4e6d60a17a3afdc0c93111276dce2522c683a172e670e8`.

The retained `renderer-evidence.log` reports that Playwright's bundled ffmpeg does not provide the `null` muxer. The repair request was retained as `awaiting-authorized-executor`; validation was not weakened to restore green.

`baca1289acdb1e4e2d066a5c43f1dd9de80eed3d` changes the existing UI dependency setup to ensure `/usr/bin/ffmpeg` is installed from the Ubuntu runner package repository and records its version. The strict full-decode verifier remains unchanged.

## Canonical main synchronization

While the candidate was under review, canonical main advanced from `d8bb64f44a7d97b38d8ac10d2193c8b1cb95f0f4` to `6baf6f0d19686cefdf1b9dea570035c13f172b97` via an unrelated TFI documentation change. GitHub compare showed ten TFI project-document paths and no overlap with the FCM/test implementation.

A two-parent merge commit `a3e5fd4bac844ad1f51c7412c9e7f47acc476b3e` was created without force-push. GitHub compare against `main@6baf6f0d19686cefdf1b9dea570035c13f172b97` then reported `behind_by=0`.

## Shopify-style affected-test follow-up

A separate follow-up PR #2513 was created for conservative Mahayana affected-test selection and then deliberately converted to Draft after discovering that #2503 is the primary CLI-first foundation. Its corrected head `a118668544b2ca21530d545034091d3b99862bb4` registered `Mahayana fast checks` run `34606303595`; because that PR changes the selector/workflow itself, the run intentionally took full fallback and all selector/Rust test-group steps succeeded. #2513 must not merge ahead of #2503 and must later be reconciled with the final `tools/fabushi-test` suite registry to avoid two drifting impact registries.

## Acceptance state

This round is not completion evidence. Creating this evidence/task update produces a newer head, so all pre-record green checks are historical. Protected merge remains blocked until the exact new head has fresh required Actions and independent review with no unresolved P1/P2. Post-main native/package/release evidence remains a separate required stage.
