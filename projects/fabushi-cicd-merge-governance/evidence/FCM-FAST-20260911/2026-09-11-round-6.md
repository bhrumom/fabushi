# Sixth round — synchronize current main and continue Shopify-like fast feedback

Project `FAB-P0003 / FCM`; task `FCM-FAST-20260911`; PR #2503.

## User continuation

The 2026-09-11 continuation asks to keep driving the automated test system toward a Shopify-like developer feedback effect: GitHub Actions owns execution, core behavior is testable through the CLI/runtime without packaging a UI, UI validation remains a thinner semantic lane, and iteration should select only the minimum safe test surface while retaining exact-source evidence.

The supplied WeChat article endpoint remains unreadable from the available retrieval path, so this record does not claim exact article parity. The implementation follows the user-stated architecture and independently verifiable upstream patterns instead of inventing details from an unreadable source.

## Upstream reference check

- Canonical Shopify CLI repository: `https://github.com/Shopify/cli`.
- Public Shopify CLI Actions expose separate PR-targeted workflows and short independent jobs rather than one indivisible all-product test. This is used only as a product-feedback reference, not as evidence about Shopify private infrastructure.
- Fabushi retains its stricter requirements that are specific to this repository: exact-SHA provenance, protected merge queue, main-only heavy package/native validation, retained failure evidence and fail-closed unknown-path handling.

## Current-main synchronization

PR #2503 had become stale after concurrent accepted work advanced canonical `main` from merge base `e2d4eda0c449e461771b855aaeee416062512f09` to `6dbd4c9a3e59c03ef2b7c3a672a6feb210840ded`.

The branch was synchronized without force-push or dropping accepted main work:

- previous PR head: `498f5d67176f390d628b43e623db37344553c002`;
- canonical main incorporated: `6dbd4c9a3e59c03ef2b7c3a672a6feb210840ded`;
- merge commit on task branch: `d99ae75708b6e71c3d5407a58ecd757a52b2c794`;
- only the concurrently advanced `projects/telegram-fabushi-integration` subtree required explicit current-main reconciliation beyond GitHub's already generated clean merge tree;
- GitHub readback after the update reports PR #2503 as mergeable on the current base.

## Fast-loop shape now under validation

1. `contract` — dependency-light controller/registry/provenance checks.
2. `core` — actual Mahayana Rust/domain/JSONL driver tests with reusable Cargo state.
3. `ui` — actual Vite renderer under headless Playwright, semantic actions, no packaged Electron binary.
4. Change-aware product gates — only affected messaging/Electron domains fan out; unknown or unprovable impact fails closed.
5. Protected merge queue — `CI result` remains the repository rule; no direct protected-main merge.
6. Canonical-main delivery — heavier desktop/native/package/release evidence remains post-merge and exact-SHA bound.

This is the intended Shopify-like feedback effect for Fabushi: most defects should be rejected by CLI/core/headless lanes before expensive full-platform packaging, while platform-specific evidence is not deleted or weakened.

## Live validation at synchronization time

A fresh exact-head Actions wave started immediately for `d99ae757...`. During this record's preparation, `Telegram media transfer` and `Electron Messenger contract` had already completed successfully; `Rust messaging core` and `Mahayana messaging host bridge` were still in progress. Therefore `d99ae757...` is not promoted to accepted merely from partial green checks.

Adding this evidence record creates a later exact branch head; all protected-integration decisions must use that later head's complete checks, never the partial `d99ae757...` state.

## Remaining acceptance

- complete exact-head `CI`, `Fabushi CLI-first fast feedback`, `Messaging Product Gate` and `Electron desktop quality gate` as selected by the changed paths;
- independent review evidence for the final head;
- protected merge-queue revalidation and protected merge;
- canonical-main readback plus the existing post-main platform/package evidence loop;
- expand suite registry and automatic repair execution in later atomic tasks rather than falsely treating this foundation as exhaustive all-platform acceptance.
