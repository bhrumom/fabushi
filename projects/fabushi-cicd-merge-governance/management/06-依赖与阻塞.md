# 依赖与阻塞

| ID | Dependency | Role | Final state |
|---|---|---|---|
| FCM-D001 | GitHub protected `main` + merge queue | final merge safety | available / verified |
| FCM-D002 | GitHub Actions metadata APIs | FCM-002 telemetry | available / verified |
| FCM-D003 | `CI result` | merge and release-source safety | available / verified |
| FCM-D004 | `Electron desktop result` | macOS release evidence | release gate enforced |
| FCM-D005 | direct `Native Android` + `Native iOS` checks | mobile release evidence | available / verified on exact source; replaces aggregation-only result for current release gates |
| FCM-D006 | GitHub-hosted runners | CI execution/latency | available; queue delay measured |
| FCM-D007 | Store credentials/signing material | actual store upload only | external operational dependency; not stored here |
| FCM-D008 | protected CI test-account Fabushi dynamic-device MCP connection | FCM-010.13 App-owned complete macOS journey | blocker: dedicated Fabushi MCP exists but `fabushi_account` and `list_devices` both return HTTP 400 `We couldn't connect your account`; expected `gha-34664585793-1-macos-app` is therefore not discoverable |

## Closure blockers

- **FCM-010.13.11:** downstream run `34664585793` successfully downloaded and installed Release `desktop-1.2.56-7ee12b790e18`, logged into the protected test account, registered the installed App-owned device, and passed the Action-owned semantic smoke. It is still waiting for the external full semantic journey. The generic device namespace exposes only existing user machines, while the dedicated Fabushi dynamic-device MCP cannot connect the required account. Therefore the run-owned `gha-34664585793-1-macos-app` cannot currently receive the required READY note, `ci_session_finish`, exact `settings-logout`, full journey actions, or final evidence-gate calls.
- Do not substitute a local Mac, historical device, KRIS, or a standalone runner agent. Restore the dedicated Fabushi MCP account connection to the protected CI test account. If the current hold expires first, rerun the same exact-source immutable Release lane after restoring that connection.

FCM-010.13 remains `in-progress`; the subsequent latency-observer/compression round remains sequenced after this blocker is closed.
