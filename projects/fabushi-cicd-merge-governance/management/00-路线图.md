# 路线图

## G0 — Fast safe merge — completed

Change-aware PR/merge-group CI, unknown-path fail-safe, queue-aware automerge, and unrelated production-deploy gating. Evidence: PR #1978, #1984, #1985.

## G1 — CI latency budget — completed

`CI latency observability` now measures real recent `CI` runs by validation surface and publishes P50/P95 + queue delay + JSON/CSV artifacts. Acceptance run `32564046852` sampled 50 runs; fast-path P95=22s against a 30s budget.

## G2 — Release train / deployment promotion — completed

Apple and Google manual store delivery now validate exact source SHA against protected `main` plus required canonical platform gates before expensive build/sign/upload. Governance contract run `32564046827` passed.

## G3 — Policy automation — completed

Narrow `.github/CODEOWNERS` covers sensitive CI/CD/release/payment surfaces without a repository-wide catch-all; `Delivery governance contract` validates ownership/release/merge invariants.

## Closure — completed

PR #1999 passed canonical CI, observer, delivery governance and portfolio governance, entered GitHub merge queue, and merged to canonical `main` as `3a39dfef0ef30f1e6ae2d53602fa862bf28ddae6`. Post-merge canonical files were re-read and verified.

## 2026-09-11 continuation — CLI-first decoupled feedback — in progress

The historical closure above covers the original G0-G3 scope only. Continue `FCM-FAST-20260911` on PR #2503: independent real Rust/CLI coverage, actual-renderer headless presentation, structured bounded repair handoff, PR reconciliation and protected exact-main delivery. Run `34559929652` proves UI 3/3 in 11.1s and all seven registered core suites successful, but core aggregate is still failed by the correctly detected native messaging lock drift. Synchronize the CI-generated lock, rerun with `--locked`, then obtain independent review/protected merge and required native/package/release evidence. Autonomous executor integration and exhaustive platform coverage remain pending; no historical closure waives them.
