# FCM-010.13.11 — Unread deselect + governance repair

- **Portfolio Project ID / Project Key:** `FAB-P0003` / `FCM`
- **Task ID:** `FCM-010.13.11-unread-deselect-governance-repair`
- **Parent:** `FCM-010.13` / `FCM-010.13.11`
- **Objective:** Close the fresh production failure on canonical `a504f50ef6c8520517e91157bcc5769527d6e30a` by establishing a real unselected assistant/list-state before the exact `unread-none` baseline probe, and synchronize the governed project control records to the latest production truth before another Release is accepted.
- **Source requirements / references:** `[Fabushi:720b2161-131e-42c6-b8d2-2c033cd35df1]`; original rapid GitHub-Actions automation objective `https://mp.weixin.qq.com/s/DEg20nFKfrd2urMc3BjntA`; parent FCM-010.13 acceptance chain; independent PR #2557 review findings `discussion_r3996054622` and `discussion_r3996054623`.
- **Status:** `IN_PROGRESS / FAIL_CLOSED`
- **Started:** `2026-09-12T11:54:00Z`
- **Updated:** `2026-09-12T11:54:00Z`
- **Completed:** `N/A — blocked until exact-head review/CI, protected merge queue, exact-source Release, and one same fresh production run satisfy the full parent gate.`

## Scope

### In scope

1. Repair the production journey precondition: before querying exact assistant `unread-none`, explicitly select a known non-assistant peer (the already-created self-hosted channel) and then return to the chat-list surface so the assistant list-only unread node is actually mounted.
2. Preserve the exact semantic identity contract `peer-unread:legacy:conversation:mahayana-ai:agent:assistant` + `unread-none` / `unread-positive`; do not weaken matching or use screenshots/OCR/private message content.
3. Strengthen the deterministic controller regression so it proves `assistant-active -> select known non-assistant peer -> chat list -> unread-none baseline -> open assistant -> send` ordering.
4. Strengthen the packaged Electron/App Surface E2E from the real assistant-active precondition that caused production failure; prove the list-only unread node is absent while assistant is selected and reappears only after selecting another peer and returning to the chat list.
5. Synchronize `management/01-WBS原子任务.md`, `management/06-依赖与阻塞.md`, `management/05-状态报告.md`, and `management/07-变更日志.md` so current project reconstruction no longer reports the obsolete missing-assistant/MOV blockers.
6. Preserve every existing gate: find limit <=100, generation retry, exact source SHA, immutable Release, exact App-owned device, all 27 categories, real non-zero remote actions, `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout`, controller/target success, truthful evidence, playable whole-session video, trace/screenshots/logs/artifacts/digest/source lineage.
7. Carry the repair through required exact-head CI, a genuinely independent no-finding review for that same exact head, protected merge queue, canonical-main re-read, exact-source immutable Release, and one brand-new controller-dispatched frozen-source production target.

### Out of scope

- Reusing targets `34680986090`, `34681709910`, `34688641913`, `34689484659`, `34691502192`, or any prior artifact as final acceptance evidence.
- Weakening semantic identity/name matching, evidence truthfulness, source/device binding, final logout ordering, protected merge, or Release immutability.
- Reworking already-verified generation retry, <=100 find contract, assistant semantic projection, semantic name, recorder implementation, OAuth/login, or package provenance except where necessary to preserve this repair.
- Running the CI latency observer before FCM-010.13.11 and FCM-010.13 are accepted by one complete fresh production run.

## Dependencies

- Canonical source at task start: `a504f50ef6c8520517e91157bcc5769527d6e30a`.
- Existing production FCM-010.13.11 controller and `macos-interactive-app-e2e` target workflows.
- Existing exact assistant/unread semantic projection and generation/find-limit protections.
- GitHub protected `main`, required CI, independent Codex review, merge queue, immutable desktop Release chain, protected test account, GitHub-hosted macOS runner, and App-owned device registration.
- Existing Playwright packaged Electron/App Surface verification layer.

## Fresh production evidence opening this task

Canonical `a504f50ef6c8520517e91157bcc5769527d6e30a` automatically progressed far enough to produce a fresh immutable-source production chain, but it failed and is diagnostic only:

- Release tag: `desktop-1.2.56-a504f50ef6c8`.
- Production controller: `34691460208` — `failure`.
- Automatically dispatched target: `34691502192`, attempt `1` — `failure`.
- Exact App-owned device: `gha-34691502192-1-macos-app`.
- Evidence artifact: `fabushi-macos-interactive-evidence-34691502192-1`.
- Artifact id: `10296494233`.
- Artifact digest: `sha256:4d06e44f654f4b571e50c349d991a97f980db1125f81289ffef7dfa377747cf6`.
- Report: `controlStatus=failed`, `remoteActionCount=26`, `playwrightOutcome=success`, `recorderPlayable=true`.
- Failure: `assistant peer unread badge cleared semantic transition timed out`, last exact query state `marker=unread-none`, generation `464`.
- Failure path truthfully emitted FAIL and `ci_session_finish`; it did **not** emit `READY_FOR_LOGOUT PASS` and therefore did not satisfy the required success-path fresh snapshot + exact settings logout.

This production failure exactly confirms the final independent PR #2557 P1: entering the `send` category after `conversations`/`search` can leave the assistant as the active peer. Merely calling `navigateSection("聊天")` does not clear that active peer, so the list-only unread semantic node remains unmounted and the exact `unread-none` query times out.

## Why PR #2557 cannot be accepted as REVIEW-PASS

PR #2557 final head `77af66957a06c26dd17dd8971eacb94bd35564d3` received an independent review, but that review produced two P1 findings rather than a pass:

1. The controller did not actually deselect the assistant before the list baseline probe.
2. WBS/blocker/status/changelog control records were stale and conflicted with newer production evidence.

The PR then merged as canonical `a504f50...`; this task does not retroactively call that review a pass and does not treat the merge as satisfying the user's REVIEW-PASS-before-merge requirement. The new repair must restore the gate on a new exact head.

## Open-source-first / reuse decision

This is the same semantic-surface ordering problem already researched in the immediately preceding unread repair. Reuse that recorded survey rather than inventing a new runtime framework: Microsoft Playwright (Apache-2.0) for stable semantic/App Surface E2E verification, Radix Primitives (MIT) as the explicit finite-state UI-state design reference, and DOM Testing Library (MIT) as semantic-query testing guidance. No new dependency or copied third-party runtime code is required. The repair uses Fabushi's existing App Surface transport plus existing Playwright packaged verification. Security/licensing impact: no new credential access, private message/unread-count exposure, screenshot parsing, dependency, source copy, or copyleft obligation.

## Branch / PR

- **Branch:** `fix/fcm-010-13-11-unread-deselect-governance`
- **PR:** pending creation from this branch to protected `main`.
- **Base at branch creation:** `a504f50ef6c8520517e91157bcc5769527d6e30a`.

## Acceptance criteria

1. Production journey explicitly establishes a non-assistant active peer before returning to chat list and querying exact `unread-none`.
2. Deterministic controller regression and packaged App Surface E2E cover the actual assistant-active precondition and repaired transition.
3. WBS, dependency/blocker register, status report, and changelog all reconstruct the latest current blocker and preserve previous rounds as history rather than current truth.
4. Required CI passes on one exact PR head.
5. A genuinely independent review explicitly covers that same exact head and returns no blocking findings; any head change invalidates the review.
6. Only after 4–5 pass may the PR enter the protected merge queue; direct protected-main merge is not acceptable.
7. After merge, re-read exact canonical `main` SHA and require an immutable desktop Release whose `target_commitish`, dereferenced tag and macOS ZIP/digest bind to that SHA.
8. Production FCM-010.13.11 controller must automatically dispatch one brand-new frozen-source target; no prior target/artifact may be reused as final evidence.
9. One same fresh run must prove exact App-owned macOS device, all 27 required categories PASS, real non-zero remote actions, exact `READY_FOR_LOGOUT PASS -> ci_session_finish -> fresh App snapshot -> exact settings-logout`, controller success, target success, and truthful evidence gate success.
10. Retain playable whole-session video, Playwright trace, step screenshots, App/system/device-call logs, reports, artifact id/digest, Release/tag/source SHA, target run/attempt and exact device lineage.
11. Only after criteria 1–10 may FCM-010.13.11 and FCM-010.13 be marked passed; only then run the CI latency observer and continue bottleneck compression.

## Verification method

- Source review of production controller transition and exact semantic query contract.
- `node --test` controller contracts, including exact ordering from assistant-active state.
- Packaged Electron/App Surface Playwright E2E with real client and assistant-selected precondition.
- Exact-head required GitHub checks and independent review readback.
- Merge-queue event/merge-group evidence plus canonical-main readback.
- Release API/tag/asset provenance inspection.
- Production controller/target logs and retained artifact inspection for device, 27 categories, remote actions, final sequence, video/trace/screenshots/logs/digest and truthful success.

## Current blockers / risks

### Blockers

1. The code/test/control-record repair is not yet implemented on this branch.
2. Exact-head required CI and independent REVIEW-PASS do not yet exist.
3. A compliant protected-queue merge, new canonical SHA, new immutable Release and brand-new accepted production target do not yet exist.

### Risks

- A source-order-only test can miss the active-peer runtime precondition; packaged E2E must reproduce assistant-selected state.
- Direct/forced merging after a P1 would repeat the #2557 governance failure; this task must stay draft/not queued until exact-head no-finding review.
- Mixing evidence from failed targets would create false lineage; acceptance remains one-run and exact-source.

## Next action

Implement only the deselect/list-state repair, corresponding deterministic + packaged regressions, and the four control-record synchronizations. Then run required exact-head CI and request a fresh independent read-only review. Do not authorize merge queue entry unless the same exact head is green and review has no blocking findings.