# 风险登记

| ID | Risk | Probability | Impact | Owner | Mitigation / trigger | Status |
|---|---|---|---|---|---|---|
| FCM-RSK-001 | path classifier misses runtime file | medium | unsafe green merge | FCM | unknown non-doc paths force all canonical checks | controlled |
| FCM-RSK-002 | merge-group diff classification wrong | low | stale/incompatible merge | FCM | official merge-group base/head + required `CI result` | controlled |
| FCM-RSK-003 | CD impact gate false-negative | low | required deploy skipped | FCM | fail safe on uncertainty; manual dispatch retained | controlled |
| FCM-RSK-004 | sensitive CI change auto-merges | low | delivery-policy regression | FCM | explicit authorization + merge queue + CODEOWNERS | controlled |
| FCM-RSK-005 | optimizing for speed reduces coverage | medium | regressions | FCM | risk-tiered selection, not test removal | controlled |
| FCM-RSK-006 | telemetry becomes a new CI blocker | low | availability/merge latency | FCM-002 | observer is soft-SLO/non-required | controlled |
| FCM-RSK-007 | store workflow publishes non-main/non-green SHA | medium | unverified release | FCM-004 | protected-main ancestry + exact-SHA required gates | controlled |
| FCM-RSK-008 | CODEOWNERS becomes too broad | medium | unnecessary review latency | FCM-005 | no catch-all; governance contract rejects broad pattern | controlled |
| FCM-RSK-009 | historical release source predates gate script | low | old release blocked | FCM-004 | supported source is post-policy protected-main history | accepted |
| FCM-RSK-010 | external controller cannot connect the protected CI test-account Fabushi device namespace, leaving the transient App-owned macOS device unreachable | low | exact-source install/smoke can succeed while the required full semantic journey times out and the truthful evidence gate fails | FCM-010.13 | preserve protected-account preflight, exact `gha-<run>-<attempt>-macos-app` binding, and nonzero remote device-call trace; never substitute local/historical/KRIS devices | closed / historical — fresh production target `34691502192` reached exact App-owned device `gha-34691502192-1-macos-app` and recorded 26 real remote actions; the old HTTP-400 diagnosis is no longer current |
| FCM-RSK-011 | assistant remains the active conversation when the controller needs the list-only exact `peer-unread:*` semantic node, or the node is queried before its remount effect settles after returning to Chats | high | `send` fails before the receive/unread probe, so the 27-category journey, READY/logout sequence and truthful success gate cannot complete | FCM-010.13.11 | production must select a known non-assistant peer before returning to Chats, then poll the exact bounded `peer-unread:legacy:conversation:mahayana-ai:agent:assistant + unread-none` query; packaged App Surface E2E must prove the assistant-active precondition, durable non-assistant `activePeerKey`, and eventual exact semantic remount without weakening identity/name matching | open / blocking |
| FCM-RSK-012 | a repair merges after a finding-bearing exact-head review or without the protected queue owning the final merge | medium | governance can accept a known-bad source and produce misleading production evidence | FCM-010.13.11 | keep PR draft until required CI and an independent no-finding review pass the same exact head; any commit invalidates review; require `main-merge-queue` / ALLGREEN and re-read canonical main after merge | open / blocking |

## Live trigger

The old protected-account connectivity trigger is closed: canonical `a504f50ef6c8520517e91157bcc5769527d6e30a` produced immutable Release `desktop-1.2.56-a504f50ef6c8`, production controller `34691460208`, and auto-dispatched target `34691502192/1`. That target reached exact App-owned device `gha-34691502192-1-macos-app`, executed 26 real remote actions, completed packaged Playwright, and retained a playable whole-session recorder. The current production failure is therefore not account/device connectivity.

Current FCM-RSK-011 trigger: target `34691502192` timed out in `send` on exact `peer-unread:legacy:conversation:mahayana-ai:agent:assistant + unread-none` because the assistant conversation remained active. The failure was truthful: FAIL -> `ci_session_finish`, no `READY_FOR_LOGOUT PASS`, no accepted fresh-snapshot/exact-logout success sequence. Artifact `10296494233`, digest `sha256:4d06e44f654f4b571e50c349d991a97f980db1125f81289ffef7dfa377747cf6`, preserves the diagnostic evidence.

The first repair head `c9ee0c88ced2e51f774c89ffc37063967bd498c3` remained fail-closed. Electron quality run `34692841431` proved the packaged regression queried the remounted unread semantic node too early after the non-assistant peer -> Chats transition (two deterministic failures at exact expected count 1 / received 0), and the independent exact-head Codex review returned a P1 because this risk register still contained the obsolete HTTP-400 blocker. That head is rejected for merge and its review cannot be reused.

FCM-RSK-011 and FCM-RSK-012 block FCM-010.13.11 / FCM-010.13 closure. The repair must pass packaged App Surface verification plus a fresh independent no-finding review on one later exact head before protected queue entry.