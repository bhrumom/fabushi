# TFI P0 CLIPPY-001 architecture diagnosis — 2026-09-05

- Project: `FAB-P0001 / TFI`.
- Parent task: `TFI-M6-P0-001`.
- New atomic task: `TFI-M6-P0-001-CLIPPY-001`.
- Execution PR: #2323, open/unmerged, diagnosed exact head `373bc52ad1cc2052c32acd81be4606c0a18dd89b`, base `9e88a2e9c030fe05147460dfa580366cf9aa433d`.
- Prior architecture authority remains historical: #2330 and #2331 are not modified or reused as this round's base.
- Input state: `EXECUTION-UNREAD-BLOCKED / NEW-SEMANTIC-FAILURE / SCOPE-EXPANSION-REQUIRED / CI-BLOCKED / CLOSURE-BLOCKED`.

## Record-truth correction
The requested path `projects/telegram-fabushi-integration/management/tasks/TFI-M6-P0-001-UNREAD-001-execution-2026-09-05.md` does not exist at #2323 head `373bc52...` (GitHub contents read returned 404). The real durable UNREAD records are:
- frozen architecture task on #2331: `management/tasks/TFI-M6-P0-001-UNREAD-001-align-group-management-fixture-with-community-authority.md`;
- execution evidence commit `02a0a261...`: `evidence/TFI-M6-P0-001/UNREAD-001-EXECUTION-2026-09-05.md`;
- evidence index commit `373bc52...`: `evidence/TFI-M6-P0-001/UNREAD-001-EXECUTION-INDEX-2026-09-05.md`.
No file is invented to conceal this mismatch.

## Current-head Actions proof
Messaging Product Gate run `33905958987`, Rust job `101131173899`, exact head `373bc52...`:
- rustfmt PASS;
- `cargo test --all-targets` PASS;
- `unread_projection_contract` 4/4 PASS, including the UNREAD-001 target;
- `m6_channels_topics_contract` 5/5 PASS;
- messaging Clippy FAIL with exactly two production diagnostics: `engine.rs:597` dead `CommunityAdminAction::PostMessages`; `service.rs:684` `clippy::collapsible_match`;
- downstream media test, media Clippy and production Feature Host bridge/contact projection SKIPPED because messaging Clippy failed;
- Product Electron job `101131173822` PASS.

Same-head additive gates: Mahayana run `33905958879` / job `101130884645` PASS; TFI Atomic `33905958904` / job `101130939914` PASS; Developer Fiat Commerce `33905958905` PASS; Explicit automerge `33905958893` PASS. None waives Product Rust.

## Reproduction proof
Semantic head `7d158e1742b2d9e56d101c90d3d81408dcd41947`, Product run `33905736673`, Rust job `101130104184`, independently shows the same sequence and exact two diagnostics after target/full tests pass. Therefore the final records commits did not create the failure and a one-off runner fault is ruled out.

## Base-vs-head and history proof
### `engine.rs`
- Audited base `9e88a2e...` already contains private enum variant `CommunityAdminAction::PostMessages` and its `require_community_admin` mapping.
- Base and current head have the same `require_community_admin` call set; none constructs `PostMessages`.
- #2323's engine patch does not add/remove the variant or a `PostMessages` call; FMT-001 only formats the file in that region.
- Historical commit `6160971cb3c477b809ae470d60f1e3c601606329` (`feat(tfi): add channel and topic management primitives`) added the private selector and mapping.
- The actual message-send path still directly checks `member.admin_rights.post_messages`. Therefore the domain right is live; only the generic private selector is dead.

Classification: `PARENT-BASE-LATENT / HISTORICAL-DEAD-PRIVATE-SELECTOR / PRODUCTION-FIX-REQUIRED`.

### `service.rs`
- Audited base `9e88a2e...` already contains the same Community-backed nested `RemoveConversationParticipant` match-arm + `if` authorization form.
- #2323 changes near the diagnostic are formatting-only; UNREAD/MOD do not alter `service.rs`.
- Historical commit `af6fb35c30f9d64d6f731c8a0d1ebef959f95a73` (`fix(messaging): make community membership canonical`) introduced this exact management authorization shape.
- The Clippy diagnostic is structural: the existing condition can move to the match-arm guard without adding/removing branches. The owner/admin predicate and denial must remain identical.

Classification: `PARENT-BASE-LATENT / HISTORICAL-PRODUCTION-CONTROL-FLOW-LINT / SEMANTICS-PRESERVING-REWRITE-REQUIRED`.

## Causality judgment
- UNREAD-001 semantic regression: **NO**. It changes one test function only; both production sites predate its starting head, and target/all-targets pass.
- MOD-001 semantic regression: **NO**. MOD changes one different test only; its 5/5 contract remains green.
- FMT-001 semantic regression: **NO**. FMT's independent review #2329 established formatter-only semantics for its three-file slice; formatting merely allowed later gates to advance.
- Parent/base latent issue: **YES, both diagnostics**.
- CI/environment issue: **NO**. Same diagnostics reproduce on two exact heads/jobs.
- Toolchain drift as root cause: **NO evidence**. The workflow intentionally runs stable Clippy with `-D warnings`; both jobs use the same stable toolchain and the lint classes are established Rust/Clippy diagnostics. Floating stable remains a general reproducibility risk, but workflow/toolchain changes are outside this task.

## Why production source must change
The gate is intentionally `cargo clippy ... --all-targets -- -D warnings`. The two warnings originate in production source and all tests already pass, so no test/fixture edit can remove them legitimately. Suppressing warnings would weaken the repository quality signal and conceal either stale private code or unnecessarily complex authorization control flow. The exact production cleanup is smaller and semantics-preserving.

## Frozen repair
One atomic task owns both diagnostics because they are the two simultaneous blockers in the same required messaging Clippy step. Splitting them would force an intermediate head that is guaranteed to remain red on the other known diagnostic without reducing semantic coupling or risk.

Allowed source surface is exactly the private `PostMessages` selector/mapping in `engine.rs` and the Community-backed `RemoveConversationParticipant` nested conditional in `service.rs`; all other source/test/workflow/config remains frozen.