# TFI P0 UNREAD-001 risk and dependency addendum — 2026-09-05

- `R-UNREAD-01` Production fallback could hide missing Community state and reopen dual membership authority. Impact HIGH. Mitigation: production `engine.rs`/`service.rs` forbidden; fixture must use existing public Community creation path.
- `R-UNREAD-02` Giving fixture admin excessive rights could weaken the test's permission value. Impact MEDIUM. Mitigation: request only `add_admins = true` needed for the existing admin-add-member path; preserve all existing negative assertions.
- `R-UNREAD-03` A one-test repair may expose another deterministic latent Product Rust failure. Impact HIGH. Mitigation: use `NEW-SEMANTIC-FAILURE` stop rule; no opportunistic scope expansion.
- `R-UNREAD-04` Green Atomic/Electron/Mahayana could be mistaken for Product Rust closure. Impact HIGH. Mitigation: required Product Rust and its downstream steps must actually execute/pass on one exact head.
- `R-UNREAD-05` Historical reviewer/FMT/MOD evidence could be overwritten. Impact HIGH. Mitigation: all new governance is append-only; prior records and #2330 are immutable history.
- `R-UNREAD-06` Cross-test/order theory could distract from deterministic state setup. Impact LOW with current evidence. Mitigation: target uses fresh `MemoryStateStore::default()`; only reclassify if Actions produce contradictory isolated/order evidence.

Dependencies:
- `DEP-UNREAD-01`: #2323 remains open/unmerged at the frozen starting head or its authorized UNREAD execution descendants.
- `DEP-UNREAD-02`: existing `UpdateCommunity` first-create semantics remain unchanged.
- `DEP-UNREAD-03`: fresh independent review depends on all required CI green.
- `DEP-UNREAD-04`: MERGE/E2E/Release/P0-002+ depend on full closeout of this blocker and subsequent review.
