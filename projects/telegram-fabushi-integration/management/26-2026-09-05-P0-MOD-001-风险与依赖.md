# TFI P0 MOD-001 risk and dependency addendum — 2026-09-05

## Active risks
- `TFI-P0-R10` **MATERIALIZED / FORMATTER PORTION CLOSED**: FMT-001 made both required formatter gates pass and exposed the later Product Rust semantic test failure. Required CI remains blocking until all downstream Rust gates execute and pass.
- `TFI-P0-R14` **HIGH — latent moderation contract mismatch**: base carries a post-ban assertion that conflicts with the state machine's active-participant projection/error precedence. Mitigation: bounded test-contract task `TFI-M6-P0-001-MOD-001`.
- `TFI-P0-R15` **CRITICAL — wrong-layer repair risk**: changing `engine.rs` error ordering or retaining `Banned` actors as active participants merely to satisfy the old assertion could weaken the Conversation↔Community single-authority invariant and expand security-sensitive semantics. Mitigation: production source is forbidden in MOD-001; any need for it is an architecture stop.
- `TFI-P0-R16` **HIGH — false semantic closure risk**: changing only the expected error without proving projection state could mask a future regression. Mitigation: the test must directly assert Community `Banned` status and absence from active Conversation participants before asserting `SenderNotParticipant`.
- `TFI-P0-R17` **HIGH — hidden next-gate risk**: Product Rust clippy/media/bridge have not run on the current head because all-targets tests fail first. Mitigation: MOD-001 acceptance requires these gates to actually execute and pass; a newly exposed deterministic failure becomes a new architecture blocker rather than scope creep.
- `TFI-P0-R13` remains **CRITICAL — false closure risk**: formatter/Mahayana/Atomic/Electron green evidence must never be represented as full required Product Rust success.

## Dependency truth
`FMT-001 (review-pass scope) -> MOD-001 -> fresh independent REVIEW-PASS + all required CI -> MERGE-001 -> E2E-001 -> independent packaged evidence/video review PASS -> RELEASE-001 -> FULL-CLOSE(TFI-M6-P0-001)`.

`TFI-M6-P0-002` remains blocked by the final FULL-CLOSE. No downstream feature, merge, E2E or release work may run in place of MOD-001.

## Stop condition
If MOD-001 cannot be completed by changing only the authorized contract test plus TFI records, or if Actions contradict the frozen state-path proof, execution stops and returns to architecture with exact run/job/log evidence. Production code must not be improvised under this task.
