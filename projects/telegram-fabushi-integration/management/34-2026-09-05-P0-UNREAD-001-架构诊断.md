# TFI P0 UNREAD-001 architecture diagnosis — 2026-09-05

- Project: `FAB-P0001 / TFI` (existing project; no new Project ID).
- Parent task: `TFI-M6-P0-001`.
- New atomic task: `TFI-M6-P0-001-UNREAD-001`.
- Execution PR: #2323, open/unmerged, exact diagnosed head `553c5efd5a6119298d0a0da8512a1ac931fcc61c`, base `9e88a2e9c030fe05147460dfa580366cf9aa433d`.
- Prior architecture authority remains historical: PR #2330 exact head `9f03b5b1e4b823a226e60bf3c791d6d6301c5521`.
- Current delivery state: `EXECUTION-MOD-BLOCKED / NEW-SEMANTIC-FAILURE / CI-BLOCKED / CLOSURE-BLOCKED`.

## Observed failure
Required Messaging Product run `33903316015`, Rust job `101122272928`, passes rustfmt and all five `m6_channels_topics_contract` tests, including the MOD-001 target. `cargo test --all-targets` then fails first at `tests/unread_projection_contract.rs::conversation_management_enforces_owner_admin_boundaries_and_removal`: helper `handle(...).unwrap()` receives `Engine(CommunityNotFound(ConversationId("group:management-contract")))`. Later messaging clippy, media test/clippy and production Feature Host bridge/contact projection are skipped. Product Electron, Mahayana fast, TFI atomic, Developer Fiat Commerce and Explicit automerge are additive green evidence only and do not waive Product Rust.

## Base-vs-head and history proof
1. `native/mahayana-messaging/tests/unread_projection_contract.rs` has the same blob `2c4f053a19dd6444448a5acab79e3d41bfe6c5d4` on audited base `9e88a2e...` and execution head `553c5e...`; MOD-001 did not modify it.
2. `ecf79c8... -> 553c5e...` changes only `m6_channels_topics_contract.rs` outside TFI project records. It does not modify `unread_projection_contract.rs`, `engine.rs`, `service.rs` or workflows.
3. The management test was added in canonical commit `ae549af580897fdf5e68db58492f76cc787eeb07` (#2251). At that commit, `Command::SetConversationParticipant` and `RemoveConversationParticipant` operated directly on Conversation participants and did not require Community state, so the fixture was valid when introduced.
4. Parent-branch commit `af6fb35c30f9d64d6f731c8a0d1ebef959f95a73` (`fix(messaging): make community membership canonical`) later changed Group/Channel participant set/remove to require an existing `CommunityState`, returning `CommunityNotFound` otherwise. The old unread-management fixture was not updated with that semantic change.
5. The current test creates a fresh `MemoryStateStore::default()`, creates actors and a Group Conversation, but never creates its Community. The service can authorize the admin from the legacy Conversation fallback when Community is absent; the engine then correctly enforces the newer Group/Channel Community prerequisite and returns the observed error.
6. `CreateConversation` creates/upserts the Conversation; it does not synthesize first Community state. The existing public `ClientCommand::UpdateCommunity` path allows the Conversation owner to establish the first Community and seeds canonical members from Conversation participants.

## Root-cause classification
- MOD-001 regression: **NO** — exact MOD delta does not touch the failing test or production membership path; its target passes.
- FMT-001/rustfmt regression: **NO** — formatting cannot create the missing domain state and the failing test is unchanged on base.
- Parent/base latent defect: **YES** — `af6fb35...` changed the required state model without migrating this pre-existing fixture.
- Fixture/test vs current state-machine contract mismatch: **YES (primary mechanism)** — a Group-management fixture still assumes Conversation-only membership after Community became canonical.
- Cross-test pollution/order dependence: **NO evidence / strongly ruled out** — the failing test creates a fresh in-memory service and deterministic local state; sibling tests pass and the failure is an explicit missing-state branch. If future Actions contradict this, execution must stop and reclassify.
- CI/environment issue: **NO** — the exact domain error follows source logic on a healthy runner after numerous tests and the MOD binary pass.

Canonical classification: **`PARENT-BASE-LATENT / STALE-FIXTURE-STATE-MACHINE-CONTRACT-MISMATCH`**.

## Minimal repair architecture
Do not weaken Community authority and do not add production fallback. Repair only the affected test fixture: after `CreateConversation`, have `human:owner` establish Community through the existing public `ClientCommand::UpdateCommunity`. The requested seed must give `human:admin` `MemberStatus::Administrator` with `AdminRights.add_admins = true` so the original test intent remains meaningful: admin may add a normal member, member may not edit metadata, owner may promote, admin may not remove another admin, owner may remove a member, and the removed member must no longer receive the Group in sync.

No direct mutation of private engine state, no test-only production API, no production source change, no workflow change, and no local build/test are authorized by this architecture round.
