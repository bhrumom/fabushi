# Windows 1.2.49 governed retest candidate

- Project: `FAB-P0001 / TFI`
- Task: `TFI-WINDOWS-RELEASE-E2E-001`
- Supersedes failed candidate: `1.2.48`
- Fix PR: `#2434`
- Fix merge SHA: `a2c25b12f64e3e8076bd456f90bd5d49a1677154`
- Candidate base: `main@a2c25b12f64e3e8076bd456f90bd5d49a1677154`
- Candidate version: `1.2.49`
- Android `androidVersionCode=29` and iOS `iosBuildNumber=29` remain unchanged.

## Why 1.2.49

Windows `1.2.48` exact-main Electron job `34027494210 / 101470965448` succeeded and produced the tested Windows installer, but same-SHA Native iOS job `34027494208 / 101470965413` failed the remote-computer close Accessibility projection. Because post-main requires same-SHA Native Android + Native iOS success, `1.2.48` is not publishable.

PR `#2434` atomically corrected only that Accessibility containment defect and protected-merged as `a2c25b12f64e3e8076bd456f90bd5d49a1677154`. The governed Windows train therefore advances strictly to `1.2.49` from that canonical merge.

## Acceptance

After this version-only protected merge, the exact accepted main SHA must complete the canonical Electron desktop quality gate and Native mobile quality gate. The post-main `workflow_run` source binding must equal that exact main SHA and its Electron run ID. Only after post-main publishes the immutable exact-source Windows installer may the Windows interactive workflow install it, authenticate the protected test account, and let the App self-register a fresh `gha-<run>-<attempt>-windows-app` device.

External released-App acceptance must use only that new App-owned Windows device; exercise all six `fabushi.app.*` semantic tools plus `ci_session_status`, `ci_session_note`, and `ci_session_finish`; cover the complete declared Windows journey including messaging, search, navigation/settings, Agent, attachments/material, sync, and final logout; make `settings-logout` the last App action; and retain whole-session video, step screenshots, device trace, Playwright trace/report/results, logs, final report, and failure evidence via always-upload.
