# TFI-MACOS-INTERACTIVE-001 — macOS 1.2.50 canonical-main retest candidate

- Project: `FAB-P0001 / TFI`
- Task: `TFI-MACOS-SEMANTIC-MESSAGE-ACTIONS-001`
- Status: `TESTING`
- Stable semantic target repair: PR `#2419`, protected merge `2ec0fbd5cc30a61e043e5050dff4a70e8a33e06e`.
- #2419 exact-head Electron desktop quality run: `34022856663` (`success`), including the >500-element stable semantic target regression.
- macOS `v1.2.47` was published successfully by run `34026918218` from `main@3ade251d5d73764255bac324d06fd788ec07edb2`, but parallel protected work advanced canonical main before final App-owned acceptance.
- Android PR `#2436` protected-merged global version `1.2.49` as `496d9f0910b1fced2eb594eded5310d5b8e5612c`.
- macOS PR `#2435` had already entered merge-group revalidation when that Android drift was observed; its queue merge completed as `49c9c4c91157898f97a8cd74e222a7d505d83fcb`, leaving canonical global version at `1.2.49`. That candidate is preserved as superseded and is not accepted as the required strict increment.
- Concurrent Windows PR `#2437` proposes `1.2.50` but, at this branch cut, has no `automerge` label and no protected merge-queue entry.
- Governed macOS candidate: strict next global version `1.2.50` from exact canonical `main@49c9c4c91157898f97a8cd74e222a7d505d83fcb`.
- Android `androidVersionCode=29` and iOS `iosBuildNumber=29` remain unchanged.
- The eight shared global version mirrors are reused byte-for-byte from the already-prepared `1.2.50` set on Windows PR `#2437`; no Windows task/evidence content is imported.

## Acceptance contract

The protected merge must preserve all existing security, semantic, account/session and release-provenance gates. If another protected version PR reaches canonical main before this macOS candidate merges, this candidate must be superseded and strictly incremented again rather than collide with the newly accepted version.

After protected merge, GitHub Actions must build, Developer-ID sign, notarize, staple, verify, hash and publish an immutable macOS prerelease from the exact accepted canonical main SHA. The release-chain must dispatch the existing macOS interactive lane. The installed Fabushi App must authenticate the protected test account and self-register a brand-new `gha-<run>-<attempt>-macos-app` device; KRIS, historical devices and runner-owned gateways are forbidden.

External acceptance must genuinely execute `fabushi.app.status`, `fabushi.app.snapshot`, `fabushi.app.find`, `fabushi.app.action`, `fabushi.app.wait`, `fabushi.app.assert`, `ci_session_status`, `ci_session_note`, and `ci_session_finish`; cover messaging, search, navigation/settings, Agent, attachments/material, sync and final `settings-logout`; and retain whole-session video, step-labelled screenshots, device-call trace, Playwright trace/report/results, application/system logs, final report and failure evidence through the always-upload path on pass or fail.
