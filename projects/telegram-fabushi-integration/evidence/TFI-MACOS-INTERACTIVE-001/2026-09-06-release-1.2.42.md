# TFI-MACOS-INTERACTIVE-001 — macOS 1.2.42 semantic-message-actions retest candidate

- Protected release base: `main@d806a734d4f7cf2816520f28a7fc3e9dac5b3849`.
- Canonical prior application version: `1.2.41`.
- Latest published macOS test release observed before this candidate: `v1.2.40` -> `9a82893c75340188c549786cbbf987b57a2e6480`.
- Failure run preserving full always-upload evidence: `34019929170`, release `v1.2.38`, App-owned device `gha-34019929170-1-macos-app`, artifact `9985464811`.
- The failure run proved bounded CI App-session renewal and device heartbeat stayed healthy; it intentionally finished FAIL because required message reply/edit/delete/forward actions were not reachable through the semantic App MCP.
- Product semantic fix: PR #2413 merged as `f1d2a69b4d365e1755befbd4f75515318b06ecdd`.
- Exact regression targeting fix: PR #2415 merged as `d806a734d4f7cf2816520f28a7fc3e9dac5b3849`; PR-head Electron quality run `34021683262` passed the real Rust Host / App Agent Surface journey.
- Candidate governed macOS test version: `1.2.42`.
- Android `androidVersionCode=29` and iOS `iosBuildNumber=29` remain unchanged.

## Release / retest contract

Only the exact protected-main merge SHA of this version PR may publish immutable prerelease `v1.2.42`. GitHub Actions must build, sign, notarize, staple, hash and publish the macOS assets. The subsequent macOS interactive Actions run must begin recording before install, log in the protected test account, and let the installed Fabushi App self-register a brand-new `gha-<run>-<attempt>-macos-app` device. KRIS, prior devices, pre-existing devices and runner-owned gateways are forbidden.

The live acceptance must genuinely call `fabushi.app.status/snapshot/find/action/wait/assert` and `ci_session_status/note/finish`; cover startup, login, main, conversations, search, send, receive, reply, edit, delete, forward, draft, pin, mute, unread, contacts, groups, bot, agent, miniapp, webmcp, media, file, notifications, sync, settings and update; then write the exact READY_FOR_LOGOUT PASS note, call `ci_session_finish`, take a fresh snapshot and invoke exact stable `settings-logout`. Always-upload evidence must retain the complete video, step screenshots, device-call trace, renewal log, Playwright report/trace/results, app/system logs and final report on pass or fail.
