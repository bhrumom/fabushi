# TFI P0 security addendum — 2026-09-04

- Project: `FAB-P0001/TFI`
- Program: `FAB-ARCH-P0-20260904`

- Community admission and membership are fail-closed; client-provided participants cannot grant membership/admin/owner rights in Community-backed conversations.
- Owner cannot be silently downgraded/removed by generic participant/community update paths. Self-leave semantics must be explicit for owner vs non-owner.
- Private Channel subscribe requires valid admission state; join request creation cannot materialize an empty Community that captures a conversation identifier.
- Invite tokens require scope, expiry/revocation/use semantics and replay negatives.
- Durable journal must not persist recipient-specific redactions or leak admin-only history. Authorization is applied when projecting for the requesting actor.
- MiniApp card payload is data, not executable HTML/JS. Open/install actions resolve through existing allowlisted Marketplace/Host contracts and immutable release validation.
- MiniApp installation cannot inject a second Bot runtime. Bot execution is delegated only to MSR policy/approval/tool boundaries.
- Group Bot privacy mode must not forward ambient group content to a Bot unless its configured trigger contract is satisfied.