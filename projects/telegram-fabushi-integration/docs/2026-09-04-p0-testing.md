# TFI P0 testing contract — 2026-09-04

- Project: `FAB-P0001/TFI`
- Program: `FAB-ARCH-P0-20260904`

## Required levels

Every task defines focused unit/contract tests plus GitHub-hosted integration checks. Local builds, native runs, E2E or full test suites are forbidden by root `AGENTS.md`; lightweight source inspection is not completion evidence.

M6 must include positive and negative contract matrices for compile/type shape, create-vs-update, owner invariant, admin downgrade, ban, leave/self-leave, public/private/invite/join_request, invite expiry/revoke/replay, recovery from persisted state, journal replay, v2 fixture decoding, v3 negotiation, server time and request correlation.

Startup must include a packaged full-relaunch simulated-user E2E using the production persistence path. Evidence must prove cached message body/content is already complete on the first usable frame and existing `startup-performance.json` remains `<1000 ms`; also collect process-launch diagnostics, trace, screenshots and full video for both pass and fail.

MiniApp card/install must include renderer contract tests, install idempotency, unavailable/yanked/invalid-card negatives, Bot projection presence, reopen after restart, and end-to-end card -> install/open -> Bot-visible journey.

Group Bot must test mention trigger, privacy-mode ignore path, reply-to-Bot trigger, command trigger, non-triggering ambient messages, scoped context continuity, typed tool-result rendering and denied-tool behavior.