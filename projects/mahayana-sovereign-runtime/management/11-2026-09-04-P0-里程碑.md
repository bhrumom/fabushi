# MSR P0 milestones — FAB-ARCH-P0-20260904

`FULL-CLOSE(X)` requires X's accepted contract, independent `REVIEW-PASS`, protected canonical-main merge, required CI, and installable/packaged E2E plus Release evidence tied to X's exact accepted canonical-main SHA.

- **M0 — governance review:** PR #2320 remains open/unmerged. Historical `REVIEW-REJECTED`, including R2 on `a5ce2e522cf124910c6627c72a646513b90960fa`, is preserved. Fresh latest-head independent review is required.
- **M1 — MSR-107:** current upstream capability/license/provenance matrix independently accepted; no runtime implementation claim.
- **M2 — session prerequisite:** `FULL-CLOSE(MSR-201)`. Current MSR-201 is `in-progress`, so M2 is not passed.
- **M3 — Bot session binding:** only after M2 may MSR-210 implementation/acceptance proceed; MSR-210 then needs its own FULL-CLOSE.
- **M4 — policy prerequisites:** `FULL-CLOSE(MSR-202)`, `FULL-CLOSE(MSR-210)`, `FULL-CLOSE(GBF-409)`, and `FULL-CLOSE(GBF-411)`. Current MSR-202 is `in-progress`; GBF-409/411 are `IN_PROGRESS`, so M4 is not passed.
- **M5 — capability policy plane:** only after M4 may MSR-211 integrate/close; it then needs its own FULL-CLOSE and fail-closed approval/stale/install/fallback evidence.
- **M6 — cross-project delivery:** dependent TFI/GBF tasks independently complete their own full closure. No downstream closure backfills unfinished MSR prerequisites.