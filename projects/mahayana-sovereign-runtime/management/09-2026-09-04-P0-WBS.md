# MSR P0 WBS — FAB-ARCH-P0-20260904

`FULL-CLOSE(X)` = contract acceptance + independent code review `REVIEW-PASS` + protected canonical-main merge + all required CI + installable/packaged E2E and Release evidence bound to X's exact accepted canonical-main SHA.

| Task | Hard dependency state | Allowed before deps | Status | Acceptance |
|---|---|---|---|---|
| MSR-107 | none | full governance/provenance audit | NOT_STARTED | exact revision/file/license/NOTICE/provenance matrix; no runtime claim |
| MSR-201 (existing) | existing task chain | existing task only | `in-progress`; commit/PR/CI pending | not FULL-CLOSE; must complete own full lineage before MSR-210 |
| MSR-202 (existing) | existing task chain | existing task only | `in-progress`; commit/PR/CI pending | not FULL-CLOSE; must complete own full lineage before MSR-211 |
| MSR-210 | `FULL-CLOSE(MSR-201)` | source reading/API/test vectors only as contract-only | BLOCKED | one Bot -> one durable session + own FULL-CLOSE |
| MSR-211 | `FULL-CLOSE(MSR-202)` + `FULL-CLOSE(MSR-210)` + `FULL-CLOSE(GBF-409)` + `FULL-CLOSE(GBF-411)` | descriptor/threat-model/test vectors only as contract-only | BLOCKED | unified capability policy/result/fallback + own FULL-CLOSE |

Current GBF facts remain: GBF-409/GBF-411 are `IN_PROGRESS`; required final CI/E2E/exact-main Release evidence is pending. No review-only or “reuse existing” state satisfies a hard gate.