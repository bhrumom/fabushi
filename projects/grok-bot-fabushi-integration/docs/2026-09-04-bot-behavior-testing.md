# GBF P0 testing — FAB-ARCH-P0-20260904

GBF-508 must prove mention/reply/command directed triggers, privacy ambient-ignore, two groups same Bot continuity, two Bots isolation, typed thinking/progress/approval/result/error states, approval deny/expire, revoked/stale device, control disabled, MiniApp installed/unavailable/uninstalled/stale, and semantic capability preferred over Computer Use.

Computer Use fallback is valid only when the accepted MSR-211/GBF-508 contracts prove the semantic/App/MiniApp capability is genuinely unavailable and same-account pairing, control enablement, target/session/generation freshness, policy and required approval are all valid. An available-but-denied semantic capability must **not** fall back to Computer Use.

Every Grok-like behavior test maps to a clean-room observable anchor ID at `107877b4...` or another rights-cleared public behavior source. No reconstructed implementation source may be copied into fixtures/tests.

## Canonical-main evidence identity
Application-affecting E2E uses GitHub Actions and an installable/package artifact. Task evidence records exact accepted main SHA, app version, platform, workflow run/job, journey/test ID, timestamp, package identity, complete video, step-labelled screenshots, trace/action evidence, HTML/native report and logs. Pass and fail upload through an `always()`-equivalent path; target retention is 90 days or the maximum allowed lower limit with the constraint recorded. Missing any required evidence blocks pass. Source-only tests do not qualify.
