# GBF P0 WBS — FAB-ARCH-P0-20260904

`FULL-CLOSE(X)` = contract acceptance + independent code review `REVIEW-PASS` + protected canonical-main merge + every required CI check + installable/packaged E2E and Release evidence bound to X's exact accepted canonical-main SHA.

| Task | Hard dependency state | Allowed before deps | Status | Closure gate |
|---|---|---|---|---|
| GBF-409 (existing) | existing task chain | continue existing task only | `IN_PROGRESS`; PR/required CI/exact-main packaged E2E/Release pending | must FULL-CLOSE before MSR-211/GBF-508 closure |
| GBF-411 (existing) | existing task chain | continue existing task only | `IN_PROGRESS`; PR #2205; GitHub CI/E2E/deployment/release/live evidence pending | must FULL-CLOSE before MSR-211/GBF-508 closure |
| MSR-210 | `FULL-CLOSE(MSR-201)` | MSR contract-only prework | BLOCKED because MSR-201 is `in-progress` | must then FULL-CLOSE itself |
| MSR-211 | `FULL-CLOSE(MSR-202)` + `FULL-CLOSE(MSR-210)` + `FULL-CLOSE(GBF-409)` + `FULL-CLOSE(GBF-411)` | MSR schema/test-vector prework only | BLOCKED | must then FULL-CLOSE itself |
| GBF-508 | `FULL-CLOSE(MSR-210)` + `FULL-CLOSE(MSR-211)` + `FULL-CLOSE(GBF-409)` + `FULL-CLOSE(GBF-411)` | clean-room anchors/spec/test vectors only as contract-only | BLOCKED | group behavior + strict fallback + own FULL-CLOSE |

MSR-201/MSR-202 remain `in-progress`; GBF-409/411 remain `IN_PROGRESS`. No review-only, downstream, or “reuse existing” evidence satisfies these task-local gates.