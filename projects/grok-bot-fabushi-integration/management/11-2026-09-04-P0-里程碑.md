# GBF P0 milestones — FAB-ARCH-P0-20260904

`FULL-CLOSE(X)` = accepted contract + independent `REVIEW-PASS` + protected canonical-main merge + required CI + installable/packaged E2E and Release evidence bound to X's exact accepted canonical-main SHA.

- **G0 — governance review:** PR #2320 remains open/unmerged; R2 `REVIEW-REJECTED` on `a5ce2e522cf124910c6627c72a646513b90960fa` remains historical truth. Fresh latest-head independent review required.
- **G1 — device foundations:** `FULL-CLOSE(GBF-409)` and `FULL-CLOSE(GBF-411)`. Both are currently `IN_PROGRESS`, so G1 is not passed.
- **G2 — session foundation:** `FULL-CLOSE(MSR-201)` then `FULL-CLOSE(MSR-210)`. MSR-201 is `in-progress`, so G2 is not passed.
- **G3 — policy foundation:** `FULL-CLOSE(MSR-202)` + G1 + `FULL-CLOSE(MSR-210)` before MSR-211; then `FULL-CLOSE(MSR-211)`. MSR-202 is `in-progress`, so G3 is not passed.
- **G4 — GBF-508:** only after `FULL-CLOSE(MSR-210/MSR-211/GBF-409/GBF-411)` may GBF-508 integrate/close; then it needs its own FULL-CLOSE and strict fallback evidence.
- **G5 — TFI integration:** TFI-M7 may consume only fully closed GBF-508/MSR contracts and must independently FULL-CLOSE with the same fallback fail-closed matrix.
- **G6 — formal delivery:** exact canonical-main package/release lineage complete for every prerequisite and dependent task. Downstream evidence never backfills a missing prerequisite lineage.