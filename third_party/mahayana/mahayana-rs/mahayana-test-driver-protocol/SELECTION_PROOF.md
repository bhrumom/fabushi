# FCM-019 leaf selection proof

This non-product documentation file exists only to prove the path-based affected-test selection contract for `mahayana-test-driver-protocol` in GitHub Actions.

Acceptance for the proof PR:

- `Mahayana fast checks` classifies this change as the `test_driver` group only.
- The presentation-neutral `mahayana-test-driver-protocol` test executes and passes.
- Unrelated Mahayana Rust groups and Linux native dependency installation remain skipped.
- The proof PR is not merged into the implementation branch or canonical `main`; its Actions run is retained as FCM-019.5 evidence.
