name: Device mesh integration

on:
  push:
    branches:
      - codex/tailscale-mobile-device-mesh
    paths:
      - .github/workflows/device-mesh-ci.yml
      - scripts/apply-device-mesh-integration.mjs
      - scripts/apply-device-mesh-canonicalization.mjs
      - scripts/apply-android-device-mesh-canonicalization.mjs
      - scripts/apply-android-native-compile-fixes.mjs
      - chatgpt-vps-control/lib/device-agent.js
      - chatgpt-vps-control/lib/device-gateway.js
      - chatgpt-vps-control/lib/device-mesh.js
      - chatgpt-vps-control/tests/device-mesh*.test.js
      - desktop/electron/remote-device-agent-supervisor.cjs
      - mobile/**
      - projects/grok-bot-fabushi-integration/**
  pull_request:
    paths:
      - .github/workflows/device-mesh-ci.yml
      - scripts/apply-device-mesh-integration.mjs
      - scripts/apply-device-mesh-canonicalization.mjs
      - scripts/apply-android-device-mesh-canonicalization.mjs
      - scripts/apply-android-native-compile-fixes.mjs
      - chatgpt-vps-control/lib/device-agent.js
      - chatgpt-vps-control/lib/device-gateway.js
      - chatgpt-vps-control/lib/device-mesh.js
      - chatgpt-vps-control/tests/device-mesh*.test.js
      - desktop/electron/remote-device-agent-supervisor.cjs
      - mobile/**

permissions:
  contents: write

concurrency:
  group: device-mesh-${{ github.ref }}
  cancel-in-progress: true

jobs:
  node-device-mesh:
    name: Signed device mesh protocol and gateway
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out exact source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: chatgpt-vps-control/package-lock.json

      - name: Apply idempotent cross-platform integration
        run: |
          node scripts/apply-device-mesh-integration.mjs
          node scripts/apply-device-mesh-canonicalization.mjs
          node scripts/apply-android-device-mesh-canonicalization.mjs
          node scripts/apply-android-native-compile-fixes.mjs

      - name: Reject whitespace and patch damage
        run: git diff --check

      - name: Install remote MCP dependencies
        run: npm ci --prefix chatgpt-vps-control

      - name: Syntax-check mesh sources and codemods
        run: |
          node --check chatgpt-vps-control/lib/device-mesh.js
          node --check chatgpt-vps-control/lib/device-agent.js
          node --check chatgpt-vps-control/lib/device-gateway.js
          node --check scripts/apply-device-mesh-integration.mjs
          node --check scripts/apply-device-mesh-canonicalization.mjs
          node --check scripts/apply-android-device-mesh-canonicalization.mjs
          node --check scripts/apply-android-native-compile-fixes.mjs

      - name: Run complete remote MCP test suite
        run: npm test --prefix chatgpt-vps-control

  android-device-mesh:
    name: Android signed device agent compile
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Check out exact source
        uses: actions/checkout@v4

      - name: Set up Node.js for generated native sources
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Apply Android mesh generation and compile fixes
        run: |
          node scripts/apply-android-device-mesh-canonicalization.mjs
          node scripts/apply-android-native-compile-fixes.mjs
          git diff --check

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          cmdline-tools-version: '14742923'
          packages: 'platform-tools'

      - name: Install Android 17 preview SDK
        shell: bash
        run: |
          set -euo pipefail
          available_packages="$(sdkmanager --list --channel=3)"
          platform_package="$(printf '%s\n' "$available_packages" | awk -F'|' '
            /platforms;android-(37([.]0)?|CinnamonBun)/ && !found {
              gsub(/^[[:space:]]+|[[:space:]]+$/, "", $1);
              print $1;
              found = 1;
            }
          ')"
          build_tools_package="$(printf '%s\n' "$available_packages" | awk -F'|' '
            /build-tools;37[.]/ && !found {
              gsub(/^[[:space:]]+|[[:space:]]+$/, "", $1);
              print $1;
              found = 1;
            }
          ')"
          test -n "$platform_package"
          test -n "$build_tools_package"
          sdkmanager --channel=3 "$platform_package" "$build_tools_package"
          if [ ! -e "$ANDROID_HOME/platforms/android-37" ]; then
            installed_platform="$(find "$ANDROID_HOME/platforms" -maxdepth 1 -type d \( -name 'android-37*' -o -name 'android-CinnamonBun*' \) | head -n 1)"
            test -n "$installed_platform"
            ln -s "$installed_platform" "$ANDROID_HOME/platforms/android-37"
          fi

      - name: Configure Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Compile and lint Android mobile mesh
        working-directory: mobile/android
        run: ./gradlew --no-daemon --build-cache :app:compileDebugKotlin :app:lintDebug

  persist-generated-integration:
    name: Persist verified generated integration
    needs: [node-device-mesh, android-device-mesh]
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out tested branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply all idempotent integrations
        run: |
          node scripts/apply-device-mesh-integration.mjs
          node scripts/apply-device-mesh-canonicalization.mjs
          node scripts/apply-android-device-mesh-canonicalization.mjs
          node scripts/apply-android-native-compile-fixes.mjs
          git diff --check

      - name: Commit verified generated sources
        shell: bash
        run: |
          if git diff --quiet; then
            echo "Device mesh integration is already committed."
            exit 0
          fi
          git config user.name "fabushi-ci[bot]"
          git config user.email "fabushi-ci[bot]@users.noreply.github.com"
          git add \
            chatgpt-vps-control/lib/device-agent.js \
            chatgpt-vps-control/lib/device-gateway.js \
            chatgpt-vps-control/lib/device-mesh.js \
            chatgpt-vps-control/tests/device-mesh-gateway.test.js \
            desktop/electron/remote-device-agent-supervisor.cjs \
            mobile/android/app/src/main/java/com/ombhrum/fabushi/FabushiDeviceMeshAgent.kt \
            mobile/android/app/src/main/java/com/ombhrum/fabushi/FabushiAppAgentSurface.kt \
            mobile/android/app/src/main/java/com/ombhrum/fabushi/FabushiScreen.kt
          git commit -m "feat(mesh): persist verified cross-platform integration"
          git push origin "HEAD:${GITHUB_REF_NAME}"

  generated-integration-clean:
    name: Generated integration is committed
    needs: [node-device-mesh, android-device-mesh]
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out pull request head
        uses: actions/checkout@v4

      - name: Require generated sources to be committed
        run: |
          node scripts/apply-device-mesh-integration.mjs
          node scripts/apply-device-mesh-canonicalization.mjs
          node scripts/apply-android-device-mesh-canonicalization.mjs
          node scripts/apply-android-native-compile-fixes.mjs
          git diff --check
          git diff --exit-code
