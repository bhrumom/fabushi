name: Legacy important release gate (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The cross-platform Important Release Gate is disabled by FCM-023.'
          echo 'Use one platform-specific Release workflow and choose test or formal explicitly.'
