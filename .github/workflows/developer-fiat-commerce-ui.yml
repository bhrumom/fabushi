name: Developer Fiat Commerce UI

on:
  push:
    branches: [main, feat/fab-p0001-dynamic-fiat-commerce]
    paths:
      - 'desktop/electron/native-edge.cjs'
      - 'desktop/electron/native-capability-handlers.cjs'
      - 'frontend/apps/web/src/app/miniapps/[id]/commerce/**'
      - '.github/scripts/apply-botfather-commerce-desktop-patch.mjs'
      - '.github/workflows/developer-fiat-commerce-ui.yml'
  pull_request:
    paths:
      - 'desktop/electron/native-edge.cjs'
      - 'desktop/electron/native-capability-handlers.cjs'
      - 'frontend/apps/web/src/app/miniapps/[id]/commerce/**'
      - '.github/workflows/developer-fiat-commerce-ui.yml'

permissions:
  contents: write

jobs:
  integrate-branch:
    if: github.event_name == 'push' && github.ref == 'refs/heads/feat/fab-p0001-dynamic-fiat-commerce'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/fab-p0001-dynamic-fiat-commerce
      - run: node .github/scripts/apply-botfather-commerce-desktop-patch.mjs
      - name: Commit native bridge integration if needed
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name 'fabushi-ci'
          git config user.email 'actions@users.noreply.github.com'
          git add desktop/electron/native-edge.cjs desktop/electron/native-capability-handlers.cjs
          git commit -m 'feat(bot-father): wire developer commerce native bridge'
          git push origin HEAD:feat/fab-p0001-dynamic-fiat-commerce

  native-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --check desktop/electron/native-edge.cjs
      - run: node --check desktop/electron/native-capability-handlers.cjs
      - name: Verify narrow commerce bridge and no caller authority
        run: |
          python3 - <<'PY'
          from pathlib import Path
          edge=Path('desktop/electron/native-edge.cjs').read_text()
          handlers=Path('desktop/electron/native-capability-handlers.cjs').read_text()
          for name in ['getDeveloperCommerceProfile','upsertDeveloperCommerceProfile','listDeveloperCommerceMiniApps','registerDeveloperCommerceMiniApp','listDeveloperCommerceProducts','createDeveloperCommerceProduct','updateDeveloperCommerceProduct','syncDeveloperCommerceGoogleProduct']:
              assert name in edge, name
              assert name in handlers, name
          create=handlers[handlers.index('async createDeveloperCommerceProduct'):handlers.index('async updateDeveloperCommerceProduct')]
          for forbidden in ['developerId', 'ownerUserId', 'platformFeeBps']:
              assert forbidden not in create, forbidden
          assert "Number.isSafeInteger(amount)" in create
          assert "2592000" in create
          PY

  web-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.9.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml
      - name: Install frontend dependencies
        working-directory: frontend
        run: pnpm install --frozen-lockfile
      - name: Build web app with Bot Father Commerce route
        working-directory: frontend
        run: pnpm build:web
