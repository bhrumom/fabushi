name: Legacy combined release (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The combined five-platform release workflow is disabled by FCM-023.'
          echo 'Use Release macOS, Release Windows, Release Linux, Release Android, or Release iOS so only the selected platform consumes runners.'
