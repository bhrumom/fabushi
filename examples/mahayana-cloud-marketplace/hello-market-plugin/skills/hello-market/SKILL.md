---
name: hello-market
description: Run the cloud marketplace hello mini app. Use when validating that a Mahayana CLI marketplace plugin was installed and can execute its hello command.
---

# Cloud Marketplace Hello

Call the `hello` tool with an optional short `name`. Confirm that the structured
result identifies `cloud-market-hello`, version `1.0.0`, and an independent
Cloudflare Worker deployment.
